# 잡담방

가볍게 이야기 나누는 브라우저 잡담방입니다.

## 기능

- 닉네임으로 입장
- 메시지 전송 / 입장·퇴장 안내
- `localStorage`에 대화 저장
- 같은 브라우저의 여러 탭에서 `BroadcastChannel`로 실시간 동기화

## 실행

```bash
cd jabdam
npm install
npm run dev
```

빌드:

```bash
npm run build
npm run preview
```
